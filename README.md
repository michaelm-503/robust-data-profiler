Build a Simple Data Profiler
- Read a CSV into a DataFrame.
- Define a function profile(df) that:
- Prints shape (rows, columns)
- Lists column names and dtypes
- Counts missing values per column
- Shows basic statistics for numeric columns (.describe())
- Shows unique counts for categorical columns

Mini Project: Robust Data Profiler
Extend your Data Profiler to:
- Accept a file path as input  
- Handle file-not-found and parse errors  
- Read/write CSV and text summary  
